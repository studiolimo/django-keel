# Env dedicato al proxy Traefik (servizio condiviso da dev e prod).
# Avvio Traefik: docker compose -f docker-compose.traefik.yml --env-file .env.traefik up -d

# Auth della dashboard Traefik (htpasswd format, generato con: htpasswd -nb admin password)
TRAEFIK_DASHBOARD_AUTH=
